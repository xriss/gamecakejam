

$( document ).ready(function() {

/*
var url="/depart"; // use base domain we loaded from but change port?

var data='{cmd="test"}';

var cb=function(suc,dat)
{
};

$.ajax({
  url: url,
  data: data,
  success: cb,
  dataType:"json"
});
*/


});
